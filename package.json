{
  "name": "cascade-screening",
  "version": "0.1.0",
  "description": "Which name matcher suffices, at which threshold, measured on your own alert history. Nothing of yours goes up.",
  "type": "module",
  "license": "PolyForm-Noncommercial-1.0.0",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "test": "tsc --noEmit && node src/readme.ts --check && node src/licences.ts --check && node --test src/*.test.ts src/*.test.mjs",
    "figures": "node src/readme.ts",
    "sceller": "node src/sceller.ts",
    "verify": "node src/verifier-rapport.mjs",
    "licences": "node src/licences.ts"
  },
  "dependencies": {
    "@huggingface/transformers": "4.2.0"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "typescript": "^5.9.0"
  }
}
